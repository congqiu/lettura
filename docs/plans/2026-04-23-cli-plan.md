# Lettura CLI（AI-first）实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 新增 `lettura-cli` 二进制 + 配套 Personal Access Token 机制 + 一份 AI skill，让 Claude Code / Cursor 等 agent 通过 HTTP API 远程消费 Lettura。

**Architecture:** Cargo workspace 拆分：`.`（现有 server crate `lettura`）+ `cli/`（新 `lettura-cli` crate，纯 HTTP 客户端）。新增 `personal_access_tokens` 表 + `PatAuthUser` 提取器与现有 JWT 并列。`save` 改造为幂等（ON CONFLICT + tag 并集）。Skill 源文件 bundled 到 CLI 也由 server `/skills/lettura.md` 动态下发。

**Tech Stack:** Rust 2024 / sqlx / axum（后端）；clap v4 / reqwest / tokio / toml / directories（CLI）；React 19 / TanStack Query（Web UI tab）。

**参考 spec:** `docs/specs/2026-04-23-cli-design.md`

**编码规范提醒（来自 CLAUDE.md）：**
- 每个模块严格 TDD（先写测试 → 确认失败 → 实现 → 确认通过 → 提交）
- 每个 commit 原子、可编译
- 测试命令：`docker compose run --rm lettura cargo test`（或在已起的容器里 `docker compose exec lettura cargo test`）
- CLI 相关测试只用 `docker compose exec lettura cargo test -p lettura-cli`
- 代码注释英文，文档中文

---

## 阶段 1：PAT 后端

### Task 1: 建 personal_access_tokens 表

**Files:**
- Create: `migrations/014_create_personal_access_tokens.sql`

- [ ] **Step 1: 写 migration**

```sql
CREATE TABLE personal_access_tokens (
    id           UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id      UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    name         TEXT NOT NULL,
    token_hash   TEXT NOT NULL UNIQUE,
    token_prefix TEXT NOT NULL,
    scope        TEXT NOT NULL CHECK (scope IN ('read', 'write')),
    last_used_at TIMESTAMPTZ,
    expires_at   TIMESTAMPTZ,
    created_at   TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE INDEX idx_pat_user ON personal_access_tokens(user_id);
CREATE INDEX idx_pat_hash ON personal_access_tokens(token_hash);
```

- [ ] **Step 2: 跑迁移验证**

```bash
./dev.sh down && ./dev.sh build
./dev.sh psql -c "\d personal_access_tokens"
```

期望：看到表结构 + 两个索引。

- [ ] **Step 3: commit**

```bash
git add migrations/014_create_personal_access_tokens.sql
git commit -m "feat(auth): add personal_access_tokens table"
```

### Task 2: PAT 数据模型 + token 生成

**Files:**
- Create: `src/models/pat.rs`
- Create: `src/models/pat_tests.rs`（或 inline `#[cfg(test)]`）
- Modify: `src/models/mod.rs`（`pub mod pat;`）

- [ ] **Step 1: 写失败测试（token 生成）**

`src/models/pat.rs`：

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn generated_token_has_lta_prefix() {
        let t = generate_token();
        assert!(t.starts_with("lta_"), "token must start with lta_");
        assert!(t.len() >= 44, "token too short: {}", t.len());
    }

    #[test]
    fn token_prefix_takes_first_12_bytes() {
        let t = "lta_abcdefghijklmnop";
        assert_eq!(token_prefix(t), "lta_abcdefgh");
    }

    #[test]
    fn hash_is_deterministic() {
        let t = "lta_sometoken";
        assert_eq!(hash_token(t), hash_token(t));
    }

    #[test]
    fn hash_differs_for_different_tokens() {
        assert_ne!(hash_token("lta_a"), hash_token("lta_b"));
    }

    #[test]
    fn generated_tokens_are_unique() {
        use std::collections::HashSet;
        let set: HashSet<_> = (0..100).map(|_| generate_token()).collect();
        assert_eq!(set.len(), 100);
    }
}
```

- [ ] **Step 2: 运行确认测试失败**

```bash
./dev.sh up -d
docker compose exec lettura cargo test -p lettura models::pat
```

期望：编译失败（函数未定义）。

- [ ] **Step 3: 实现**

```rust
use rand::Rng;
use sha2::{Digest, Sha256};

const TOKEN_PREFIX: &str = "lta_";
const TOKEN_BODY_LEN: usize = 40;

pub fn generate_token() -> String {
    const CHARSET: &[u8] = b"ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789";
    let mut rng = rand::thread_rng();
    let body: String = (0..TOKEN_BODY_LEN)
        .map(|_| CHARSET[rng.gen_range(0..CHARSET.len())] as char)
        .collect();
    format!("{TOKEN_PREFIX}{body}")
}

pub fn token_prefix(token: &str) -> String {
    token.chars().take(12).collect()
}

pub fn hash_token(token: &str) -> String {
    let mut h = Sha256::new();
    h.update(token.as_bytes());
    hex::encode(h.finalize())
}

#[derive(Debug, Clone, sqlx::FromRow, serde::Serialize)]
pub struct PersonalAccessToken {
    pub id: uuid::Uuid,
    pub user_id: uuid::Uuid,
    pub name: String,
    pub token_prefix: String,
    pub scope: String,
    pub last_used_at: Option<chrono::DateTime<chrono::Utc>>,
    pub expires_at: Option<chrono::DateTime<chrono::Utc>>,
    pub created_at: chrono::DateTime<chrono::Utc>,
}
```

- [ ] **Step 4: 跑通测试**

```bash
docker compose exec lettura cargo test -p lettura models::pat
```

期望：5 PASS。

- [ ] **Step 5: commit**

```bash
git add src/models/pat.rs src/models/mod.rs
git commit -m "feat(auth): add PAT model with token generation and hashing"
```

### Task 3: PAT DAO（insert / lookup / list / delete / touch）

**Files:**
- Modify: `src/models/pat.rs`
- Test: 在 `src/models/pat.rs` 的 `#[cfg(test)]` 块内（用 `sqlx::PgPool` fixture，参考 `tests/common/mod.rs`）

- [ ] **Step 1: 写 DB 测试**（先看 `tests/common/mod.rs` 里的 `TestDb` 用法）

Create integration test `tests/pat_dao.rs`：

```rust
mod common;
use common::TestDb;
use lettura::models::pat::{self, Scope};

#[tokio::test]
async fn insert_then_lookup_by_hash_returns_row() {
    let db = TestDb::new().await;
    let user = common::create_test_user(&db.pool).await;
    let token = pat::generate_token();
    let hash = pat::hash_token(&token);
    let prefix = pat::token_prefix(&token);

    let id = pat::insert(&db.pool, user.id, "laptop", &hash, &prefix, Scope::Write, None)
        .await
        .unwrap();

    let row = pat::find_by_hash(&db.pool, &hash).await.unwrap().unwrap();
    assert_eq!(row.id, id);
    assert_eq!(row.user_id, user.id);
    assert_eq!(row.scope, "write");
}

#[tokio::test]
async fn list_for_user_returns_only_that_users_tokens() {
    let db = TestDb::new().await;
    let u1 = common::create_test_user(&db.pool).await;
    let u2 = common::create_test_user_with_email(&db.pool, "u2@e.com").await;

    pat::insert(&db.pool, u1.id, "u1a", "h1", "lta_h1", Scope::Read, None).await.unwrap();
    pat::insert(&db.pool, u1.id, "u1b", "h2", "lta_h2", Scope::Write, None).await.unwrap();
    pat::insert(&db.pool, u2.id, "u2a", "h3", "lta_h3", Scope::Read, None).await.unwrap();

    let u1_list = pat::list_for_user(&db.pool, u1.id).await.unwrap();
    assert_eq!(u1_list.len(), 2);
}

#[tokio::test]
async fn delete_removes_row() {
    let db = TestDb::new().await;
    let user = common::create_test_user(&db.pool).await;
    let id = pat::insert(&db.pool, user.id, "x", "h", "lta_h", Scope::Read, None).await.unwrap();

    assert!(pat::delete(&db.pool, user.id, id).await.unwrap());
    assert!(pat::find_by_hash(&db.pool, "h").await.unwrap().is_none());
}

#[tokio::test]
async fn find_expired_token_returns_none() {
    let db = TestDb::new().await;
    let user = common::create_test_user(&db.pool).await;
    let past = chrono::Utc::now() - chrono::Duration::days(1);
    pat::insert(&db.pool, user.id, "x", "h", "lta_h", Scope::Read, Some(past)).await.unwrap();

    let row = pat::find_valid_by_hash(&db.pool, "h").await.unwrap();
    assert!(row.is_none(), "expired token should not be returned");
}
```

- [ ] **Step 2: 确认测试失败**

```bash
docker compose exec lettura cargo test --test pat_dao
```

- [ ] **Step 3: 实现 DAO**

```rust
pub enum Scope { Read, Write }

impl Scope {
    pub fn as_str(&self) -> &'static str {
        match self { Scope::Read => "read", Scope::Write => "write" }
    }
}

pub async fn insert(
    pool: &sqlx::PgPool, user_id: uuid::Uuid, name: &str,
    hash: &str, prefix: &str, scope: Scope,
    expires_at: Option<chrono::DateTime<chrono::Utc>>,
) -> Result<uuid::Uuid, ModelError> {
    let rec: (uuid::Uuid,) = sqlx::query_as(
        "INSERT INTO personal_access_tokens (user_id, name, token_hash, token_prefix, scope, expires_at)
         VALUES ($1,$2,$3,$4,$5,$6) RETURNING id",
    )
    .bind(user_id).bind(name).bind(hash).bind(prefix).bind(scope.as_str()).bind(expires_at)
    .fetch_one(pool).await?;
    Ok(rec.0)
}

pub async fn find_by_hash(pool: &sqlx::PgPool, hash: &str) -> Result<Option<PersonalAccessToken>, ModelError> {
    Ok(sqlx::query_as("SELECT * FROM personal_access_tokens WHERE token_hash = $1")
        .bind(hash).fetch_optional(pool).await?)
}

/// Like `find_by_hash` but returns None for expired tokens.
pub async fn find_valid_by_hash(pool: &sqlx::PgPool, hash: &str) -> Result<Option<PersonalAccessToken>, ModelError> {
    Ok(sqlx::query_as(
        "SELECT * FROM personal_access_tokens
         WHERE token_hash = $1 AND (expires_at IS NULL OR expires_at > now())",
    ).bind(hash).fetch_optional(pool).await?)
}

pub async fn list_for_user(pool: &sqlx::PgPool, user_id: uuid::Uuid) -> Result<Vec<PersonalAccessToken>, ModelError> {
    Ok(sqlx::query_as(
        "SELECT * FROM personal_access_tokens WHERE user_id = $1 ORDER BY created_at DESC",
    ).bind(user_id).fetch_all(pool).await?)
}

pub async fn delete(pool: &sqlx::PgPool, user_id: uuid::Uuid, id: uuid::Uuid) -> Result<bool, ModelError> {
    let rows = sqlx::query("DELETE FROM personal_access_tokens WHERE id = $1 AND user_id = $2")
        .bind(id).bind(user_id).execute(pool).await?.rows_affected();
    Ok(rows > 0)
}

/// Best-effort last_used_at update; swallow errors (not auth-critical).
pub async fn touch_last_used(pool: &sqlx::PgPool, id: uuid::Uuid) {
    let _ = sqlx::query(
        "UPDATE personal_access_tokens SET last_used_at = now() WHERE id = $1
         AND (last_used_at IS NULL OR last_used_at < now() - INTERVAL '60 seconds')",
    ).bind(id).execute(pool).await;
}
```

- [ ] **Step 4: 跑测试**

```bash
docker compose exec lettura cargo test --test pat_dao
```

期望：4 PASS。

- [ ] **Step 5: commit**

```bash
git add src/models/pat.rs tests/pat_dao.rs
git commit -m "feat(auth): PAT DAO (insert/find/list/delete/touch)"
```

### Task 4: PAT 鉴权提取器

**Files:**
- Modify: `src/auth/middleware.rs`（在 `AuthUser::from_request_parts` 里加 PAT 分支）
- Test: `tests/pat_auth.rs`

- [ ] **Step 1: 写鉴权集成测试**

```rust
mod common;
use common::TestDb;

#[tokio::test]
async fn pat_bearer_authenticates_and_injects_user() {
    let db = TestDb::new().await;
    let user = common::create_test_user(&db.pool).await;
    let token = lettura::models::pat::generate_token();
    let hash = lettura::models::pat::hash_token(&token);
    let prefix = lettura::models::pat::token_prefix(&token);
    lettura::models::pat::insert(
        &db.pool, user.id, "test", &hash, &prefix,
        lettura::models::pat::Scope::Write, None,
    ).await.unwrap();

    let app = common::build_app(db.pool.clone()).await;
    let resp = common::authed_get(&app, "/api/v1/entries", &token).await;
    assert_eq!(resp.status(), 200, "PAT should authenticate like JWT");
}

#[tokio::test]
async fn read_scope_blocks_write_request() {
    let db = TestDb::new().await;
    let user = common::create_test_user(&db.pool).await;
    let token = lettura::models::pat::generate_token();
    lettura::models::pat::insert(
        &db.pool, user.id, "test",
        &lettura::models::pat::hash_token(&token),
        &lettura::models::pat::token_prefix(&token),
        lettura::models::pat::Scope::Read, None,
    ).await.unwrap();

    let app = common::build_app(db.pool.clone()).await;
    let resp = common::authed_post_json(&app, "/api/v1/entries",
        serde_json::json!({"url":"https://x.test"}), &token).await;
    assert_eq!(resp.status(), 403);
}

#[tokio::test]
async fn expired_pat_returns_401() { /* 类似 Task 3 过期 case */ }

#[tokio::test]
async fn unknown_pat_returns_401() { /* 随机 lta_ 值 */ }

#[tokio::test]
async fn jwt_still_works_alongside_pat() {
    // 用 JWT 打 /api/v1/entries，期望仍 200
}
```

- [ ] **Step 2: 修改 `src/auth/middleware.rs`**

```rust
use axum::extract::FromRequestParts;
use axum::http::{Method, request::Parts};

use crate::api::error::ApiError;
use crate::auth::jwt;
use crate::models::pat;
use crate::state::AppState;

#[derive(Debug, Clone)]
pub struct AuthUser {
    pub user_id: uuid::Uuid,
    pub is_admin: bool,
    pub source: AuthSource,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub enum AuthSource {
    Jwt,
    Pat { scope: PatScope, token_id: uuid::Uuid },
}

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum PatScope { Read, Write }

impl AuthSource {
    pub fn allows_write(&self) -> bool {
        match self {
            AuthSource::Jwt => true,
            AuthSource::Pat { scope: PatScope::Write, .. } => true,
            AuthSource::Pat { scope: PatScope::Read, .. } => false,
        }
    }
}

impl FromRequestParts<AppState> for AuthUser {
    type Rejection = ApiError;

    async fn from_request_parts(parts: &mut Parts, state: &AppState) -> Result<Self, Self::Rejection> {
        let token = parts.headers.get("Authorization")
            .and_then(|v| v.to_str().ok())
            .and_then(|s| s.strip_prefix("Bearer "))
            .ok_or_else(|| ApiError::Unauthorized("missing authorization header".into()))?;

        let user = if let Some(body) = token.strip_prefix("lta_") {
            let _ = body; // reserved
            let hash = pat::hash_token(token);
            let row = pat::find_valid_by_hash(&state.pool, &hash).await
                .map_err(|e| ApiError::Internal(e.to_string()))?
                .ok_or_else(|| ApiError::Unauthorized("invalid or expired token".into()))?;
            let scope = match row.scope.as_str() {
                "write" => PatScope::Write,
                _ => PatScope::Read,
            };
            // enforce scope by HTTP method
            if !matches!(parts.method, Method::GET | Method::HEAD) && scope == PatScope::Read {
                return Err(ApiError::Forbidden("token scope 'read' cannot perform write".into()));
            }
            pat::touch_last_used(&state.pool, row.id).await;
            // admin capability requires JWT login
            AuthUser {
                user_id: row.user_id,
                is_admin: false,
                source: AuthSource::Pat { scope, token_id: row.id },
            }
        } else {
            let claims = jwt::validate_token(token, &state.config.jwt_secret)
                .map_err(|e| ApiError::Unauthorized(e.to_string()))?;
            AuthUser {
                user_id: claims.sub,
                is_admin: claims.is_admin,
                source: AuthSource::Jwt,
            }
        };

        Ok(user)
    }
}
```

- [ ] **Step 3: 确认 `ApiError::Forbidden` 存在，不存在则补**

`src/api/error.rs` 若无 `Forbidden` 变体，加：

```rust
Forbidden(String),  // 对应 403
// 在 IntoResponse impl 里映射到 StatusCode::FORBIDDEN
```

- [ ] **Step 4: 跑测试**

```bash
docker compose exec lettura cargo test --test pat_auth
```

期望：5 PASS。如果有 test 用到 `common::build_app` / `common::authed_*` 但现有 `tests/common/mod.rs` 没这些 helper，本 step 同时补进 common。

- [ ] **Step 5: commit**

```bash
git add src/auth/middleware.rs src/api/error.rs tests/pat_auth.rs tests/common/mod.rs
git commit -m "feat(auth): authenticate Bearer tokens with lta_ prefix as PAT"
```

### Task 5: Tokens 管理 API

**Files:**
- Create: `src/api/tokens.rs`
- Modify: `src/api/mod.rs`（挂路由 + import）
- Test: `tests/tokens_api.rs`

- [ ] **Step 1: 写 API 集成测试**

覆盖 happy path + 重要边界：

```rust
// 1. JWT 登录后 POST /api/v1/tokens → 返回 token 明文
// 2. 用刚生成的 token 打 GET /api/v1/tokens → 401（PAT 不可管 PAT）
// 3. 再 JWT GET /api/v1/tokens → 看到一条，prefix 对齐，无 token 明文
// 4. DELETE /api/v1/tokens/:id → 204；再 list → 空
// 5. POST 时 scope 非法 → 400
// 6. POST 时 expires_in_days = 30 → expires_at 大约 = now + 30d
```

- [ ] **Step 2: 实现 handler**

```rust
use axum::{Json, extract::{Path, State}, http::StatusCode};
use serde::{Deserialize, Serialize};

use crate::auth::middleware::{AuthUser, AuthSource};
use crate::models::pat::{self, Scope};
use crate::api::error::ApiError;
use crate::state::AppState;

#[derive(Deserialize)]
pub struct CreateTokenRequest {
    pub name: String,
    pub scope: String,
    pub expires_in_days: Option<i64>,
}

#[derive(Serialize)]
pub struct CreateTokenResponse {
    pub id: uuid::Uuid,
    pub name: String,
    pub scope: String,
    pub token: String,  // plaintext, only here
}

fn require_jwt(auth: &AuthUser) -> Result<(), ApiError> {
    match auth.source {
        AuthSource::Jwt => Ok(()),
        AuthSource::Pat { .. } => Err(ApiError::Forbidden(
            "token management requires interactive login".into(),
        )),
    }
}

pub async fn create_token(
    State(state): State<AppState>, auth: AuthUser,
    Json(req): Json<CreateTokenRequest>,
) -> Result<(StatusCode, Json<CreateTokenResponse>), ApiError> {
    require_jwt(&auth)?;
    let scope = match req.scope.as_str() {
        "read" => Scope::Read,
        "write" => Scope::Write,
        _ => return Err(ApiError::BadRequest("scope must be 'read' or 'write'".into())),
    };
    if req.name.trim().is_empty() || req.name.len() > 64 {
        return Err(ApiError::BadRequest("name: 1..=64 chars".into()));
    }
    let expires_at = req.expires_in_days.map(|d| chrono::Utc::now() + chrono::Duration::days(d));

    let token = pat::generate_token();
    let hash = pat::hash_token(&token);
    let prefix = pat::token_prefix(&token);
    let scope_str = match scope { Scope::Read => "read", Scope::Write => "write" }.to_string();
    let id = pat::insert(&state.pool, auth.user_id, &req.name, &hash, &prefix, scope, expires_at)
        .await
        .map_err(|e| ApiError::Internal(e.to_string()))?;

    Ok((StatusCode::CREATED, Json(CreateTokenResponse {
        id, name: req.name, scope: scope_str, token,
    })))
}

pub async fn list_tokens(State(state): State<AppState>, auth: AuthUser) -> Result<Json<Vec<pat::PersonalAccessToken>>, ApiError> {
    require_jwt(&auth)?;
    Ok(Json(pat::list_for_user(&state.pool, auth.user_id).await.map_err(|e| ApiError::Internal(e.to_string()))?))
}

pub async fn delete_token(State(state): State<AppState>, auth: AuthUser, Path(id): Path<uuid::Uuid>) -> Result<StatusCode, ApiError> {
    require_jwt(&auth)?;
    if pat::delete(&state.pool, auth.user_id, id).await.map_err(|e| ApiError::Internal(e.to_string()))? {
        Ok(StatusCode::NO_CONTENT)
    } else {
        Err(ApiError::NotFound("token".into()))
    }
}
```

- [ ] **Step 3: 挂路由**

`src/api/mod.rs`，`.route(...)` 链条里加：

```rust
.route("/api/v1/tokens", get(tokens::list_tokens).post(tokens::create_token))
.route("/api/v1/tokens/{id}", delete(tokens::delete_token))
```

并 `pub mod tokens;`。

- [ ] **Step 4: 跑测试**

```bash
docker compose exec lettura cargo test --test tokens_api
```

- [ ] **Step 5: commit**

```bash
git add src/api/tokens.rs src/api/mod.rs tests/tokens_api.rs
git commit -m "feat(api): tokens management endpoints (create/list/delete)"
```

---

## 阶段 2：Tokens Web UI

### Task 6: web API 客户端 + types

**Files:**
- Modify: `web/src/lib/api.ts`（或等价 API 封装处）— 在仓库中先 `grep -R "fetch.*api/v1" web/src/lib` 对齐调用风格
- Create: `web/src/lib/tokens.ts`（若现有 api 层按 domain 拆分，则放这里）

- [ ] **Step 1: 加类型 + 调用**

```ts
export interface TokenRow {
  id: string; name: string; scope: 'read' | 'write';
  prefix: string; last_used_at: string | null;
  expires_at: string | null; created_at: string;
}

export interface CreatedToken extends Pick<TokenRow, 'id' | 'name' | 'scope'> {
  token: string;
}

export async function listTokens(): Promise<TokenRow[]> { /* GET /api/v1/tokens */ }
export async function createToken(body: { name: string; scope: 'read'|'write'; expires_in_days: number|null }): Promise<CreatedToken> { /* POST */ }
export async function deleteToken(id: string): Promise<void> { /* DELETE */ }
```

- [ ] **Step 2: commit**

```bash
git add web/src/lib/tokens.ts web/src/lib/api.ts
git commit -m "feat(web): API client for tokens endpoints"
```

### Task 7: Tokens 设置页

**Files:**
- Create: `web/src/components/settings/TokensPanel.tsx`
- Create: `web/src/components/settings/GenerateTokenDialog.tsx`
- Modify: `web/src/pages/SettingsPage.tsx`（加 Tab）

- [ ] **Step 1: TokensPanel.tsx — 列表 + 删除**

```tsx
import { useQuery, useQueryClient } from '@tanstack/react-query';
import { listTokens, deleteToken, type TokenRow } from '@/lib/tokens';
import { GenerateTokenDialog } from './GenerateTokenDialog';
import { useState } from 'react';

export function TokensPanel() {
  const qc = useQueryClient();
  const { data: rows = [], isLoading } = useQuery({ queryKey: ['tokens'], queryFn: listTokens });
  const [dialogOpen, setDialogOpen] = useState(false);

  async function handleRevoke(id: string) {
    if (!confirm('Revoke this token? The CLI session using it will stop working.')) return;
    await deleteToken(id);
    qc.invalidateQueries({ queryKey: ['tokens'] });
  }

  if (isLoading) return <div>Loading…</div>;
  if (rows.length === 0) return <EmptyState onNew={() => setDialogOpen(true)} dialogOpen={dialogOpen} setDialogOpen={setDialogOpen} />;

  return (
    <section>
      <header className="flex items-center justify-between mb-4">
        <h2 className="text-xl font-semibold">API Tokens</h2>
        <button onClick={() => setDialogOpen(true)} className="btn-primary">Generate token</button>
      </header>
      <table className="w-full text-sm">
        <thead><tr><th>Name</th><th>Prefix</th><th>Scope</th><th>Last used</th><th>Expires</th><th></th></tr></thead>
        <tbody>
          {rows.map(r => (
            <tr key={r.id}>
              <td>{r.name}</td>
              <td><code>{r.prefix}…</code></td>
              <td>{r.scope}</td>
              <td>{r.last_used_at ? new Date(r.last_used_at).toLocaleString() : '—'}</td>
              <td>{r.expires_at ? new Date(r.expires_at).toLocaleDateString() : 'Never'}</td>
              <td><button onClick={() => handleRevoke(r.id)} className="text-red-600">Revoke</button></td>
            </tr>
          ))}
        </tbody>
      </table>
      <GenerateTokenDialog open={dialogOpen} onClose={() => setDialogOpen(false)} onCreated={() => qc.invalidateQueries({ queryKey: ['tokens'] })} />
    </section>
  );
}

function EmptyState({ onNew, dialogOpen, setDialogOpen }: {
  onNew: () => void; dialogOpen: boolean; setDialogOpen: (b: boolean) => void;
}) {
  return (
    <div className="p-8 text-center space-y-3">
      <h2 className="text-xl font-semibold">No API tokens yet</h2>
      <p>Generate one to let the <code>lettura-cli</code> (or any HTTP client) access your data.</p>
      <button onClick={onNew} className="btn-primary">Generate token</button>
      <pre className="text-left bg-gray-100 p-3 rounded text-xs">curl -sSL https://lettura.dev/install.sh | sh
lettura-cli login</pre>
      <GenerateTokenDialog open={dialogOpen} onClose={() => setDialogOpen(false)} onCreated={() => { /* react-query invalidate */ }} />
    </div>
  );
}
```

- [ ] **Step 2: GenerateTokenDialog.tsx**

```tsx
import { useState } from 'react';
import { createToken, type CreatedToken } from '@/lib/tokens';

export function GenerateTokenDialog({ open, onClose, onCreated }: {
  open: boolean; onClose: () => void; onCreated: () => void;
}) {
  const [name, setName] = useState('');
  const [scope, setScope] = useState<'read'|'write'>('write');
  const [expires, setExpires] = useState<number | null>(null);
  const [created, setCreated] = useState<CreatedToken | null>(null);
  const [busy, setBusy] = useState(false);

  async function handleSubmit(e: React.FormEvent) {
    e.preventDefault();
    setBusy(true);
    try {
      const t = await createToken({ name, scope, expires_in_days: expires });
      setCreated(t);
      onCreated();
    } finally { setBusy(false); }
  }

  if (!open) return null;

  if (created) {
    return (
      <Modal onClose={() => { setCreated(null); onClose(); }}>
        <h3>Token created</h3>
        <p><strong>This is the only time you'll see this token.</strong> Copy it now.</p>
        <code className="block p-3 bg-yellow-50 border border-yellow-300 font-mono text-sm break-all">{created.token}</code>
        <button onClick={() => navigator.clipboard.writeText(created.token)}>Copy</button>
        <button onClick={() => { setCreated(null); onClose(); }}>Done</button>
      </Modal>
    );
  }

  return (
    <Modal onClose={onClose}>
      <form onSubmit={handleSubmit} className="space-y-3">
        <h3>Generate API token</h3>
        <label>Name <input required maxLength={64} value={name} onChange={e => setName(e.target.value)} /></label>
        <label>Scope
          <select value={scope} onChange={e => setScope(e.target.value as any)}>
            <option value="write">write (full access)</option>
            <option value="read">read (list/get/search only)</option>
          </select>
        </label>
        <label>Expires
          <select value={expires ?? ''} onChange={e => setExpires(e.target.value ? Number(e.target.value) : null)}>
            <option value="">Never</option>
            <option value={30}>30 days</option>
            <option value={90}>90 days</option>
            <option value={365}>1 year</option>
          </select>
        </label>
        <button type="submit" disabled={busy || !name}>Generate</button>
        <button type="button" onClick={onClose}>Cancel</button>
      </form>
    </Modal>
  );
}

function Modal({ children, onClose }: { children: React.ReactNode; onClose: () => void }) {
  return (
    <div className="fixed inset-0 bg-black/40 flex items-center justify-center z-50" onClick={onClose}>
      <div className="bg-white p-6 rounded max-w-lg w-full" onClick={e => e.stopPropagation()}>{children}</div>
    </div>
  );
}
```

- [ ] **Step 3: 挂到 SettingsPage 作为新 tab**

参照现有 SettingsPage tab 模式，加 `{key: 'tokens', label: 'API Tokens', render: () => <TokensPanel />}`。

- [ ] **Step 4: 浏览器手动验证**

```bash
./dev.sh up -d
# 访问 http://localhost:3330/settings → Tokens tab
# 生成一个 token，确认：
#   - 明文只出现一次
#   - 刷新后列表还在，且看不到明文
#   - revoke 后从列表消失
```

- [ ] **Step 5: commit**

```bash
git add web/src/components/settings/TokensPanel.tsx web/src/components/settings/GenerateTokenDialog.tsx web/src/pages/SettingsPage.tsx
git commit -m "feat(web): API Tokens settings panel with generate dialog"
```

---

## 阶段 3：Cargo workspace + CLI 骨架

### Task 8: 拆 Cargo workspace

**Files:**
- Modify: `Cargo.toml`（加 `[workspace]` 段）
- Create: `cli/Cargo.toml`
- Create: `cli/src/main.rs`

- [ ] **Step 1: 根 Cargo.toml 顶部加 workspace**

```toml
[workspace]
members = [".", "cli"]
resolver = "2"

[workspace.package]
edition = "2024"
version = "0.1.0"

[package]
name = "lettura"
version.workspace = true
edition.workspace = true
# ...现有内容不变
```

- [ ] **Step 2: 创建 cli/Cargo.toml**

```toml
[package]
name = "lettura-cli"
version.workspace = true
edition.workspace = true

[[bin]]
name = "lettura-cli"
path = "src/main.rs"

[dependencies]
anyhow = "1"
clap = { version = "4", features = ["derive", "env"] }
reqwest = { version = "0.12", default-features = false, features = ["json", "rustls-tls"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["rt-multi-thread", "macros", "fs", "io-std"] }
toml = "0.8"
directories = "5"
chrono = { version = "0.4", features = ["serde"] }
rust-embed = "8"
url = "2"
uuid = { version = "1", features = ["v4", "serde"] }

[dev-dependencies]
assert_cmd = "2"
predicates = "3"
tempfile = "3"
httpmock = "0.7"
```

- [ ] **Step 3: 最小 main.rs**

```rust
fn main() {
    println!("lettura-cli (placeholder)");
}
```

- [ ] **Step 4: 确认两个 crate 都能编译**

```bash
docker compose exec lettura cargo build -p lettura-cli
docker compose exec lettura cargo build -p lettura
```

- [ ] **Step 5: commit**

```bash
git add Cargo.toml Cargo.lock cli/Cargo.toml cli/src/main.rs
git commit -m "chore: convert repo to Cargo workspace with lettura-cli member"
```

### Task 9: CLI 骨架 — clap + 全局 flag

**Files:**
- Modify: `cli/src/main.rs`
- Create: `cli/src/cli.rs`

- [ ] **Step 1: `cli/src/cli.rs` — clap parser**

```rust
use clap::{Parser, Subcommand, ValueEnum};

#[derive(Parser)]
#[command(name = "lettura-cli", version, about = "AI-first CLI for Lettura")]
pub struct Cli {
    #[arg(long, global = true, env = "LETTURA_PROFILE")]
    pub profile: Option<String>,
    #[arg(long, global = true, env = "LETTURA_URL")]
    pub url: Option<String>,
    #[arg(long, global = true, env = "LETTURA_TOKEN", hide_env_values = true)]
    pub token: Option<String>,
    #[arg(long, global = true, value_enum, default_value = "json")]
    pub output: OutputFormat,
    #[arg(long, global = true)]
    pub quiet: bool,
    #[arg(long, global = true)]
    pub pretty: bool,
    #[command(subcommand)]
    pub cmd: Command,
}

#[derive(Clone, Copy, ValueEnum)]
pub enum OutputFormat { Json, Ids, Human }

#[derive(Subcommand)]
pub enum Command {
    Login,
    Whoami,
    Config { #[command(subcommand)] cmd: ConfigCmd },

    List(ListArgs),
    Search(SearchArgs),
    Get(GetArgs),

    Save(SaveArgs),
    Tag(TagArgs),
    Untag(UntagArgs),
    Archive(StateChangeArgs),
    Star(StateChangeArgs),

    Tags,
    Skill { #[command(subcommand)] cmd: SkillCmd },
}

#[derive(Subcommand)]
pub enum ConfigCmd {
    Get { key: String },
    Set { key: String, value: String },
    List,
}

#[derive(Subcommand)]
pub enum SkillCmd {
    Print,
    Install { #[arg(long)] path: Option<String> },
}

// Args structs filled in later tasks; declare now with TODOs is forbidden.
// Each task that adds a command will define its Args struct here.
#[derive(clap::Args)] pub struct ListArgs { #[arg(long)] pub filter: Option<String>, #[arg(long)] pub limit: Option<i64>, #[arg(long)] pub fields: Option<String> }
#[derive(clap::Args)] pub struct SearchArgs { pub query: String, #[arg(long)] pub limit: Option<i64> }
#[derive(clap::Args)] pub struct GetArgs { pub id: String, #[arg(long, value_enum, default_value = "markdown")] pub format: GetFormat }
#[derive(Clone, Copy, ValueEnum)] pub enum GetFormat { Markdown, Json, Html, Text }
#[derive(clap::Args)] pub struct SaveArgs { pub url: String, #[arg(long)] pub title: Option<String>, #[arg(long, value_delimiter = ',')] pub tag: Vec<String>, #[arg(long)] pub wait: bool }
#[derive(clap::Args)] pub struct TagArgs {
    pub id: Option<String>, pub names: Vec<String>,
    #[arg(long, value_delimiter = ',')] pub add: Vec<String>,
    #[arg(long)] pub filter: Option<String>,
    #[arg(long)] pub dry_run: bool, #[arg(long)] pub yes: bool,
}
#[derive(clap::Args)] pub struct UntagArgs {
    pub id: Option<String>, pub names: Vec<String>,
    #[arg(long, value_delimiter = ',')] pub remove: Vec<String>,
    #[arg(long)] pub filter: Option<String>,
    #[arg(long)] pub dry_run: bool, #[arg(long)] pub yes: bool,
}
#[derive(clap::Args)] pub struct StateChangeArgs {
    pub id: Option<String>,
    #[arg(long)] pub filter: Option<String>,
    #[arg(long)] pub dry_run: bool, #[arg(long)] pub yes: bool,
}
```

- [ ] **Step 2: main.rs 派发（暂时全部 `todo!()`）**

```rust
mod cli;

use clap::Parser;
use cli::{Cli, Command};

#[tokio::main]
async fn main() {
    let args = Cli::parse();
    let code = run(args).await;
    std::process::exit(code);
}

async fn run(args: Cli) -> i32 {
    match args.cmd {
        Command::Login | Command::Whoami | Command::Config { .. } => { todo!("phase 4") }
        Command::List(_) | Command::Search(_) | Command::Get(_) | Command::Tags => { todo!("phase 5") }
        Command::Save(_) | Command::Tag(_) | Command::Untag(_) | Command::Archive(_) | Command::Star(_) => { todo!("phase 6") }
        Command::Skill { .. } => { todo!("phase 7") }
    }
}
```

- [ ] **Step 3: 验证 `--help` 展示所有命令**

```bash
docker compose exec lettura cargo run -p lettura-cli -- --help
docker compose exec lettura cargo run -p lettura-cli -- save --help
```

- [ ] **Step 4: commit**

```bash
git add cli/src/
git commit -m "feat(cli): clap scaffolding with all subcommands declared"
```

### Task 10: config 文件 IO

**Files:**
- Create: `cli/src/config.rs`
- Create: `cli/tests/config.rs`

- [ ] **Step 1: 写测试**

```rust
use lettura_cli::config::{Config, resolve, Override};
use tempfile::tempdir;

#[test]
fn resolves_default_profile() {
    let dir = tempdir().unwrap();
    let path = dir.path().join("config.toml");
    std::fs::write(&path, r#"
default_profile = "default"
[profiles.default]
url = "https://x.test"
token = "lta_abc"
"#).unwrap();
    let cfg = Config::load_from(&path).unwrap();
    let r = resolve(&cfg, &Override::default()).unwrap();
    assert_eq!(r.url, "https://x.test");
    assert_eq!(r.token, "lta_abc");
}

#[test]
fn cli_override_wins_over_profile() {
    let dir = tempdir().unwrap();
    let path = dir.path().join("config.toml");
    std::fs::write(&path, r#"
default_profile = "default"
[profiles.default]
url = "https://x.test"
token = "lta_abc"
"#).unwrap();
    let cfg = Config::load_from(&path).unwrap();
    let r = resolve(&cfg, &Override { url: Some("https://override.test".into()), ..Default::default() }).unwrap();
    assert_eq!(r.url, "https://override.test");
    assert_eq!(r.token, "lta_abc");
}

#[test]
fn missing_config_returns_ok_none() {
    let dir = tempdir().unwrap();
    let res = Config::load_from(&dir.path().join("nope.toml"));
    assert!(res.unwrap().profiles.is_empty());
}

#[test]
fn save_sets_0600_permissions_on_unix() {
    #[cfg(unix)]
    {
        use std::os::unix::fs::PermissionsExt;
        let dir = tempdir().unwrap();
        let path = dir.path().join("config.toml");
        let mut cfg = Config::default();
        cfg.profiles.insert("default".into(), lettura_cli::config::Profile {
            url: "https://x.test".into(), token: "lta_abc".into(),
        });
        cfg.default_profile = Some("default".into());
        cfg.save_to(&path).unwrap();
        let mode = std::fs::metadata(&path).unwrap().permissions().mode() & 0o777;
        assert_eq!(mode, 0o600);
    }
}
```

- [ ] **Step 2: 实现 config.rs**

```rust
use std::collections::BTreeMap;
use std::path::Path;
use serde::{Deserialize, Serialize};

#[derive(Debug, Default, Deserialize, Serialize)]
pub struct Config {
    pub default_profile: Option<String>,
    #[serde(default)]
    pub profiles: BTreeMap<String, Profile>,
}

#[derive(Debug, Clone, Deserialize, Serialize)]
pub struct Profile {
    pub url: String,
    pub token: String,
}

#[derive(Debug, Default)]
pub struct Override {
    pub profile: Option<String>,
    pub url: Option<String>,
    pub token: Option<String>,
}

#[derive(Debug, Clone)]
pub struct Resolved {
    pub url: String,
    pub token: String,
}

impl Config {
    pub fn default_path() -> anyhow::Result<std::path::PathBuf> {
        let dirs = directories::ProjectDirs::from("dev", "lettura", "lettura")
            .ok_or_else(|| anyhow::anyhow!("no config dir"))?;
        Ok(dirs.config_dir().join("config.toml"))
    }

    pub fn load_from(path: &Path) -> anyhow::Result<Self> {
        if !path.exists() { return Ok(Self::default()); }
        let s = std::fs::read_to_string(path)?;
        Ok(toml::from_str(&s)?)
    }

    pub fn save_to(&self, path: &Path) -> anyhow::Result<()> {
        if let Some(p) = path.parent() { std::fs::create_dir_all(p)?; }
        let s = toml::to_string_pretty(self)?;
        std::fs::write(path, s)?;
        #[cfg(unix)]
        {
            use std::os::unix::fs::PermissionsExt;
            std::fs::set_permissions(path, std::fs::Permissions::from_mode(0o600))?;
        }
        Ok(())
    }
}

pub fn resolve(cfg: &Config, over: &Override) -> anyhow::Result<Resolved> {
    let profile_name = over.profile.clone().or(cfg.default_profile.clone());
    let profile = profile_name.as_ref().and_then(|n| cfg.profiles.get(n));

    let url = over.url.clone()
        .or_else(|| profile.map(|p| p.url.clone()))
        .ok_or_else(|| anyhow::anyhow!("no server URL configured. Run `lettura-cli login`."))?;
    let token = over.token.clone()
        .or_else(|| profile.map(|p| p.token.clone()))
        .ok_or_else(|| anyhow::anyhow!("no token configured. Run `lettura-cli login`."))?;
    Ok(Resolved { url, token })
}
```

- [ ] **Step 3: 跑测试**

```bash
docker compose exec lettura cargo test -p lettura-cli --test config
```

- [ ] **Step 4: 把 config 导出**：`cli/src/main.rs` 顶加 `pub mod config;`，并把 `mod cli;` 改成 `pub mod cli;`，再同时建一个 `cli/src/lib.rs`：

```rust
pub mod config;
pub mod cli;
pub mod client;    // placeholder module, filled next task
```

main.rs 改成用 crate name import 即可（`use lettura_cli::cli::...`）。

- [ ] **Step 5: commit**

```bash
git add cli/src/ cli/tests/config.rs
git commit -m "feat(cli): config file IO with profile resolution and 0600 perms"
```

### Task 11: HTTP 客户端 + 错误映射

**Files:**
- Create: `cli/src/client.rs`
- Create: `cli/src/error.rs`
- Create: `cli/tests/client.rs`（用 `httpmock`）

- [ ] **Step 1: 写错误类型 + 退出码映射**

```rust
// cli/src/error.rs
use serde::Serialize;

#[derive(Debug, Clone, Copy)]
pub enum ExitCode {
    Success = 0,
    NotFound = 2,
    Unauthorized = 3,
    BadArgs = 4,
    ServerError = 5,
    RateLimited = 6,
    Conflict = 7,
}

#[derive(Debug, thiserror::Error)]
pub enum CliError {
    #[error("not_found: {0}")]
    NotFound(String),
    #[error("unauthorized: {0}")]
    Unauthorized(String),
    #[error("forbidden: {0}")]
    Forbidden(String),
    #[error("bad_args: {0}")]
    BadArgs(String),
    #[error("server_error: {0}")]
    ServerError(String),
    #[error("rate_limited: {0}")]
    RateLimited { retry_after_sec: Option<u64>, message: String },
    #[error("conflict: {0}")]
    Conflict(String),
    #[error("network: {0}")]
    Network(String),
}

impl CliError {
    pub fn exit_code(&self) -> ExitCode {
        match self {
            Self::NotFound(_) => ExitCode::NotFound,
            Self::Unauthorized(_) | Self::Forbidden(_) => ExitCode::Unauthorized,
            Self::BadArgs(_) => ExitCode::BadArgs,
            Self::ServerError(_) | Self::Network(_) => ExitCode::ServerError,
            Self::RateLimited { .. } => ExitCode::RateLimited,
            Self::Conflict(_) => ExitCode::Conflict,
        }
    }

    pub fn code_name(&self) -> &'static str {
        match self {
            Self::NotFound(_) => "not_found",
            Self::Unauthorized(_) => "unauthorized",
            Self::Forbidden(_) => "forbidden",
            Self::BadArgs(_) => "bad_args",
            Self::ServerError(_) => "server_error",
            Self::RateLimited { .. } => "rate_limited",
            Self::Conflict(_) => "conflict",
            Self::Network(_) => "network",
        }
    }

    pub fn hint(&self) -> Option<String> {
        match self {
            Self::Unauthorized(_) => Some("Run `lettura-cli login` to refresh your credentials.".into()),
            Self::RateLimited { retry_after_sec: Some(s), .. } => Some(format!("Retry after {s} seconds.")),
            Self::NotFound(_) => Some("Use `lettura-cli list` to find entry ids.".into()),
            _ => None,
        }
    }
}

#[derive(Serialize)]
pub struct ErrorReport<'a> {
    pub error: ErrorBody<'a>,
}

#[derive(Serialize)]
pub struct ErrorBody<'a> {
    pub code: &'a str,
    pub message: String,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub hint: Option<String>,
}

pub fn emit_error_to_stderr(err: &CliError) {
    let report = ErrorReport {
        error: ErrorBody { code: err.code_name(), message: err.to_string(), hint: err.hint() },
    };
    let _ = serde_json::to_writer(std::io::stderr(), &report);
    eprintln!();
}
```

- [ ] **Step 2: HTTP client**

```rust
// cli/src/client.rs
use reqwest::{Client, StatusCode, header::{HeaderMap, HeaderValue, AUTHORIZATION, USER_AGENT}};
use serde::{de::DeserializeOwned, Serialize};

use crate::error::CliError;

pub struct ApiClient {
    http: Client,
    base: String,
}

impl ApiClient {
    pub fn new(base: String, token: &str) -> anyhow::Result<Self> {
        let mut headers = HeaderMap::new();
        headers.insert(AUTHORIZATION, HeaderValue::from_str(&format!("Bearer {token}"))?);
        headers.insert(USER_AGENT, HeaderValue::from_static(
            concat!("lettura-cli/", env!("CARGO_PKG_VERSION"))));
        let http = Client::builder().default_headers(headers).build()?;
        Ok(Self { http, base: base.trim_end_matches('/').to_string() })
    }

    pub async fn get<T: DeserializeOwned>(&self, path: &str, query: &[(&str, String)]) -> Result<T, CliError> {
        let resp = self.http.get(format!("{}{}", self.base, path))
            .query(query).send().await.map_err(|e| CliError::Network(e.to_string()))?;
        handle_response(resp).await
    }

    pub async fn post<B: Serialize, T: DeserializeOwned>(&self, path: &str, body: &B) -> Result<T, CliError> {
        let resp = self.http.post(format!("{}{}", self.base, path))
            .json(body).send().await.map_err(|e| CliError::Network(e.to_string()))?;
        handle_response(resp).await
    }

    pub async fn delete<T: DeserializeOwned>(&self, path: &str) -> Result<T, CliError> {
        let resp = self.http.delete(format!("{}{}", self.base, path))
            .send().await.map_err(|e| CliError::Network(e.to_string()))?;
        handle_response(resp).await
    }

    pub async fn get_text(&self, path: &str, query: &[(&str, String)]) -> Result<String, CliError> {
        let resp = self.http.get(format!("{}{}", self.base, path))
            .query(query).send().await.map_err(|e| CliError::Network(e.to_string()))?;
        let status = resp.status();
        let body = resp.text().await.map_err(|e| CliError::Network(e.to_string()))?;
        if status.is_success() { Ok(body) } else { Err(map_status(status, body)) }
    }
}

async fn handle_response<T: DeserializeOwned>(resp: reqwest::Response) -> Result<T, CliError> {
    let status = resp.status();
    let body = resp.text().await.map_err(|e| CliError::Network(e.to_string()))?;
    if status == StatusCode::NO_CONTENT {
        return serde_json::from_str("null").map_err(|e| CliError::ServerError(e.to_string()));
    }
    if status.is_success() {
        return serde_json::from_str(&body).map_err(|e| CliError::ServerError(format!("bad json: {e}")));
    }
    Err(map_status(status, body))
}

fn map_status(status: StatusCode, body: String) -> CliError {
    match status.as_u16() {
        401 => CliError::Unauthorized(body),
        403 => CliError::Forbidden(body),
        404 => CliError::NotFound(body),
        400 | 422 => CliError::BadArgs(body),
        409 => CliError::Conflict(body),
        429 => CliError::RateLimited { retry_after_sec: None, message: body },
        500..=599 => CliError::ServerError(body),
        _ => CliError::ServerError(format!("HTTP {status}: {body}")),
    }
}
```

- [ ] **Step 3: httpmock 集成测试**

`cli/tests/client.rs` 覆盖：成功 / 401 / 404 / 500 / 无网络。

- [ ] **Step 4: 跑测试**

```bash
docker compose exec lettura cargo test -p lettura-cli --test client
```

- [ ] **Step 5: commit**

```bash
git add cli/src/client.rs cli/src/error.rs cli/tests/client.rs
git commit -m "feat(cli): reqwest client with error taxonomy and exit codes"
```

### Task 12: login / whoami / config 子命令

**Files:**
- Create: `cli/src/commands/mod.rs`
- Create: `cli/src/commands/login.rs`
- Create: `cli/src/commands/whoami.rs`
- Create: `cli/src/commands/config.rs`
- Modify: `cli/src/main.rs`（dispatch）
- Modify: `cli/src/lib.rs`（`pub mod commands;`）

- [ ] **Step 1: 确认 server 有 `/api/v1/auth/me`**（若无，加 handler 返回 `{user_id, username, email, is_admin, auth_source}`）

```bash
grep -n "auth/me\|whoami" /Users/work/code/workspace/lettura/src/api/auth.rs /Users/work/code/workspace/lettura/src/api/mod.rs
```

如果不存在，本 task 先在 server 端加：

```rust
// src/api/auth.rs
pub async fn me(State(state): State<AppState>, auth: AuthUser) -> Result<Json<MeResponse>, ApiError> {
    let (username, email) = sqlx::query_as::<_, (String, String)>(
        "SELECT username, email FROM users WHERE id = $1"
    ).bind(auth.user_id).fetch_one(&state.pool).await.map_err(|e| ApiError::Internal(e.to_string()))?;
    Ok(Json(MeResponse {
        user_id: auth.user_id, username, email, is_admin: auth.is_admin,
        auth_source: match auth.source { AuthSource::Jwt => "jwt", AuthSource::Pat { .. } => "pat" }.into(),
    }))
}
```

路由：`.route("/api/v1/auth/me", get(auth::me))`。

- [ ] **Step 2: login handler**

```rust
pub async fn run(args: &Cli) -> Result<i32, CliError> {
    use std::io::{BufRead, Write};
    let mut stdout = std::io::stdout();

    let existing = Config::load_from(&Config::default_path()?).unwrap_or_default();
    let default_url = existing.default_profile.as_ref()
        .and_then(|n| existing.profiles.get(n))
        .map(|p| p.url.as_str()).unwrap_or("");

    write!(stdout, "Server URL [{}]: ", default_url)?; stdout.flush()?;
    let mut url = String::new(); std::io::stdin().lock().read_line(&mut url)?;
    let url = url.trim().to_string();
    let url = if url.is_empty() { default_url.to_string() } else { url };
    if url.is_empty() { return Err(CliError::BadArgs("URL required".into())); }

    // 探测 /api/health
    let probe = reqwest::get(format!("{}/api/health", url.trim_end_matches('/'))).await
        .map_err(|e| CliError::Network(format!("cannot reach {url}: {e}")))?;
    if !probe.status().is_success() {
        return Err(CliError::Network(format!("health check failed: HTTP {}", probe.status())));
    }

    println!("Open {}/settings → API Tokens → Generate token", url.trim_end_matches('/'));
    write!(stdout, "Paste token: ")?; stdout.flush()?;
    let mut token = String::new(); std::io::stdin().lock().read_line(&mut token)?;
    let token = token.trim().to_string();
    if !token.starts_with("lta_") {
        return Err(CliError::BadArgs("token must start with lta_".into()));
    }

    // 验证
    let client = ApiClient::new(url.clone(), &token)?;
    let _me: serde_json::Value = client.get("/api/v1/auth/me", &[]).await?;

    let profile_name = args.profile.clone().unwrap_or_else(|| "default".into());
    let mut cfg = existing;
    cfg.default_profile.get_or_insert(profile_name.clone());
    cfg.profiles.insert(profile_name.clone(), Profile { url, token });
    cfg.save_to(&Config::default_path()?)?;

    println!("Saved profile '{profile_name}'.");
    Ok(0)
}
```

- [ ] **Step 3: whoami**

```rust
pub async fn run(resolved: &Resolved) -> Result<i32, CliError> {
    let client = ApiClient::new(resolved.url.clone(), &resolved.token)?;
    let me: serde_json::Value = client.get("/api/v1/auth/me", &[]).await?;
    println!("{}", serde_json::to_string_pretty(&me).unwrap());
    Ok(0)
}
```

- [ ] **Step 4: config get/set/list**

```rust
pub fn run(cmd: &ConfigCmd) -> Result<i32, CliError> {
    let path = Config::default_path()?;
    let mut cfg = Config::load_from(&path).unwrap_or_default();
    match cmd {
        ConfigCmd::List => { println!("{}", toml::to_string_pretty(&cfg).unwrap()); }
        ConfigCmd::Get { key } => {
            let v = read_key(&cfg, key).ok_or_else(|| CliError::NotFound(format!("key {key}")))?;
            println!("{v}");
        }
        ConfigCmd::Set { key, value } => {
            write_key(&mut cfg, key, value)?;
            cfg.save_to(&path)?;
        }
    }
    Ok(0)
}

// read_key/write_key 支持 dotted path: "default_profile" / "profiles.default.url" / "profiles.default.token"
```

- [ ] **Step 5: main.rs 派发这三个命令**

- [ ] **Step 6: E2E 手动验证**

```bash
# 起 server + 手动创建 token（通过 web UI）
docker compose exec lettura cargo run -p lettura-cli -- login
docker compose exec lettura cargo run -p lettura-cli -- whoami
docker compose exec lettura cargo run -p lettura-cli -- config list
```

- [ ] **Step 7: commit**

```bash
git add src/api/auth.rs src/api/mod.rs cli/src/commands/ cli/src/main.rs cli/src/lib.rs
git commit -m "feat(cli): login / whoami / config commands"
```

---

## 阶段 4：CLI 读取类命令

### Task 13: Filter DSL 解析器

**Files:**
- Create: `cli/src/filter.rs`
- Create: `cli/tests/filter.rs`

Filter 语法是 CLI 侧的 DSL，但最终转成 server `/api/v1/entries` 已有的 query string（参见 `src/models/entry.rs` `ListParams`）。DSL 能表达但 server 不支持的部分（如 `!tag:x`、`untagged`、`since:7d`、`older-than:90d`），Task 14 会在 server 侧扩展 `ListParams`。

- [ ] **Step 1: 写测试**

```rust
use lettura_cli::filter::{parse, Filter};

#[test]
fn parses_single_tag() {
    let f = parse("tag:golang").unwrap();
    assert_eq!(f.tags_include, vec!["golang".to_string()]);
}

#[test]
fn parses_negated_tag() {
    let f = parse("!tag:archive").unwrap();
    assert_eq!(f.tags_exclude, vec!["archive".to_string()]);
}

#[test]
fn parses_untagged() {
    let f = parse("untagged").unwrap();
    assert!(f.untagged);
}

#[test]
fn parses_multi_and() {
    let f = parse("domain:medium.com,untagged,since:7d").unwrap();
    assert_eq!(f.domain, Some("medium.com".into()));
    assert!(f.untagged);
    assert!(f.since.is_some());
}

#[test]
fn parses_absolute_since() {
    let f = parse("since:2026-01-01").unwrap();
    let expect = chrono::NaiveDate::from_ymd_opt(2026,1,1).unwrap().and_hms_opt(0,0,0).unwrap().and_utc();
    assert_eq!(f.since.unwrap(), expect);
}

#[test]
fn parses_relative_older_than() {
    let f = parse("older-than:90d").unwrap();
    assert!(f.older_than.is_some());
}

#[test]
fn parses_boolean_flags() {
    let f = parse("starred,!archived,unread").unwrap();
    assert_eq!(f.starred, Some(true));
    assert_eq!(f.archived, Some(false));
    assert_eq!(f.read, Some(false));
}

#[test]
fn parses_search() {
    let f = parse("search:rust async").unwrap();
    assert_eq!(f.search, Some("rust async".into()));
}

#[test]
fn rejects_unknown_key() {
    assert!(parse("banana:1").is_err());
}
```

- [ ] **Step 2: 实现**

```rust
use chrono::{DateTime, Utc};

#[derive(Debug, Default, PartialEq)]
pub struct Filter {
    pub tags_include: Vec<String>,
    pub tags_exclude: Vec<String>,
    pub untagged: bool,
    pub domain: Option<String>,
    pub since: Option<DateTime<Utc>>,
    pub older_than: Option<DateTime<Utc>>,
    pub starred: Option<bool>,
    pub archived: Option<bool>,
    pub read: Option<bool>,
    pub search: Option<String>,
}

pub fn parse(input: &str) -> anyhow::Result<Filter> {
    let mut f = Filter::default();
    for raw in input.split(',') {
        let part = raw.trim();
        if part.is_empty() { continue; }
        let (neg, p) = if let Some(r) = part.strip_prefix('!') { (true, r) } else { (false, part) };
        if let Some(v) = p.strip_prefix("tag:") {
            if neg { f.tags_exclude.push(v.into()); } else { f.tags_include.push(v.into()); }
        } else if p == "untagged" { f.untagged = true; }
        else if let Some(v) = p.strip_prefix("domain:") { f.domain = Some(v.into()); }
        else if let Some(v) = p.strip_prefix("since:") { f.since = Some(parse_time_point(v)?); }
        else if let Some(v) = p.strip_prefix("older-than:") { f.older_than = Some(parse_time_point(v)?); }
        else if p == "starred" { f.starred = Some(!neg); }
        else if p == "archived" { f.archived = Some(!neg); }
        else if p == "unread" { f.read = Some(false); }
        else if p == "read" { f.read = Some(true); }
        else if let Some(v) = p.strip_prefix("search:") { f.search = Some(v.into()); }
        else { anyhow::bail!("unknown filter key: {part}"); }
    }
    Ok(f)
}

fn parse_time_point(s: &str) -> anyhow::Result<DateTime<Utc>> {
    // relative: 7d / 24h / 2w
    if let Some(rest) = s.chars().last() {
        if matches!(rest, 'h'|'d'|'w') {
            let num: i64 = s[..s.len()-1].parse()?;
            let dur = match rest { 'h' => chrono::Duration::hours(num), 'd' => chrono::Duration::days(num), 'w' => chrono::Duration::weeks(num), _ => unreachable!() };
            return Ok(Utc::now() - dur);
        }
    }
    // absolute: ISO date or datetime
    if let Ok(d) = chrono::NaiveDate::parse_from_str(s, "%Y-%m-%d") {
        return Ok(d.and_hms_opt(0,0,0).unwrap().and_utc());
    }
    Ok(DateTime::parse_from_rfc3339(s)?.with_timezone(&Utc))
}

impl Filter {
    /// Convert to HTTP query params for /api/v1/entries.
    pub fn to_query(&self) -> Vec<(&'static str, String)> {
        let mut q = vec![];
        if !self.tags_include.is_empty() { q.push(("tag", self.tags_include.join(","))); }
        if !self.tags_exclude.is_empty() { q.push(("exclude_tag", self.tags_exclude.join(","))); }
        if self.untagged { q.push(("untagged", "true".into())); }
        if let Some(d) = &self.domain { q.push(("domain", d.clone())); }
        if let Some(t) = self.since { q.push(("since", t.to_rfc3339())); }
        if let Some(t) = self.older_than { q.push(("before", t.to_rfc3339())); }
        if let Some(b) = self.starred { q.push(("is_starred", b.to_string())); }
        if let Some(b) = self.archived { q.push(("is_archived", b.to_string())); }
        if let Some(b) = self.read { q.push(("is_read", b.to_string())); }
        if let Some(s) = &self.search { q.push(("search", s.clone())); }
        q
    }
}
```

- [ ] **Step 3: 跑测试**

```bash
docker compose exec lettura cargo test -p lettura-cli --test filter
```

- [ ] **Step 4: commit**

```bash
git add cli/src/filter.rs cli/tests/filter.rs
git commit -m "feat(cli): filter DSL parser with to_query mapping"
```

### Task 14: server 侧 ListParams 扩展

**Files:**
- Modify: `src/models/entry.rs`（`ListParams` + `list_entries` SQL）
- Modify: `src/api/entries.rs`（handler pass-through）
- Test: `tests/entries_filter.rs`

- [ ] **Step 1: 扩 `ListParams`**

```rust
pub struct ListParams {
    pub page: Option<i64>, pub per_page: Option<i64>,
    pub is_archived: Option<bool>, pub is_starred: Option<bool>,
    pub is_read: Option<bool>,
    pub tag: Option<String>,           // comma-separated, AND
    pub exclude_tag: Option<String>,
    pub untagged: Option<bool>,
    pub domain: Option<String>,
    pub since: Option<DateTime<Utc>>,
    pub before: Option<DateTime<Utc>>,
    pub search: Option<String>,
    pub fields: Option<String>,
}
```

- [ ] **Step 2: SQL 改造（动态构建 `QueryBuilder`）**

用 `sqlx::QueryBuilder` 累加 WHERE 子句，避免字符串拼接。关键片段：

```rust
let mut qb = sqlx::QueryBuilder::new("SELECT … FROM entries WHERE user_id = ");
qb.push_bind(user_id);
qb.push(" AND deleted_at IS NULL");

if let Some(true) = p.untagged {
    qb.push(" AND NOT EXISTS (SELECT 1 FROM entry_tags et WHERE et.entry_id = entries.id)");
}
if let Some(tags_csv) = &p.tag {
    for t in tags_csv.split(',').filter(|s| !s.is_empty()) {
        qb.push(" AND EXISTS (SELECT 1 FROM entry_tags et JOIN tags tg ON tg.id = et.tag_id WHERE et.entry_id = entries.id AND tg.label = ");
        qb.push_bind(t.to_string()); qb.push(")");
    }
}
// ...exclude_tag, since, before, domain, is_starred, is_archived, is_read, search (search 走已有 FTS path)
```

- [ ] **Step 3: 测试**

`tests/entries_filter.rs` 建 3 个 entry，打不同 filter 组合，断言返回 id 集合。

- [ ] **Step 4: commit**

```bash
git add src/models/entry.rs src/api/entries.rs tests/entries_filter.rs
git commit -m "feat(entries): extend list API with untagged/exclude_tag/since/before/domain"
```

### Task 15: CLI list / search / get

**Files:**
- Create: `cli/src/commands/list.rs`
- Create: `cli/src/commands/search.rs`
- Create: `cli/src/commands/get.rs`
- Create: `cli/src/commands/tags.rs`
- Create: `cli/src/output.rs`（统一 JSON / ids / human 渲染）
- Create: `cli/tests/list_cmd.rs` （用 httpmock）
- Modify: `cli/src/main.rs`（派发）

- [ ] **Step 1: 响应类型**

```rust
// cli/src/api_types.rs
use serde::{Deserialize, Serialize};

#[derive(Debug, Deserialize, Serialize)]
pub struct EntrySummary {
    pub id: uuid::Uuid,
    pub url: String,
    pub given_url: Option<String>,
    pub title: Option<String>,
    pub domain: Option<String>,
    #[serde(default)] pub tags: Vec<String>,
    #[serde(default)] pub is_starred: bool,
    #[serde(default)] pub is_archived: bool,
    #[serde(default)] pub is_read: bool,
    pub saved_at: Option<chrono::DateTime<chrono::Utc>>,
    pub reading_time_minutes: Option<i32>,
    pub word_count: Option<i32>,
}

#[derive(Debug, Deserialize, Serialize)]
pub struct EntryFull {
    #[serde(flatten)] pub summary: EntrySummary,
    pub content_markdown: Option<String>,
    pub content_html: Option<String>,
    pub content_text: Option<String>,
    pub language: Option<String>,
    pub status: Option<String>,  // queued/ready/failed
}
```

- [ ] **Step 2: output.rs**

```rust
pub fn emit_json<T: serde::Serialize>(v: &T, pretty: bool) -> anyhow::Result<()> {
    let out = std::io::stdout();
    if pretty { serde_json::to_writer_pretty(out.lock(), v)?; } else { serde_json::to_writer(out.lock(), v)?; }
    println!();
    Ok(())
}

pub fn emit_ids(ids: impl IntoIterator<Item = uuid::Uuid>) {
    for id in ids { println!("{id}"); }
}
```

- [ ] **Step 3: list**

```rust
pub async fn run(client: &ApiClient, args: &ListArgs, output: OutputFormat, pretty: bool) -> Result<i32, CliError> {
    let filter = args.filter.as_deref().map(filter::parse).transpose()
        .map_err(|e| CliError::BadArgs(e.to_string()))?.unwrap_or_default();
    let mut q = filter.to_query();
    q.push(("per_page", args.limit.unwrap_or(20).to_string()));
    if let Some(fields) = &args.fields { q.push(("fields", fields.clone())); }

    let entries: Vec<EntrySummary> = client.get("/api/v1/entries", &q).await?;
    match output {
        OutputFormat::Ids => emit_ids(entries.into_iter().map(|e| e.id)),
        OutputFormat::Json => emit_json(&entries, pretty)?,
        OutputFormat::Human => render_human_table(&entries),
    }
    Ok(0)
}
```

- [ ] **Step 4: search**（调 `/api/v1/entries/search?q=…`，格式同 list）

- [ ] **Step 5: get**（调 `/api/v1/entries/{id}`；按 `--format` 分流）

```rust
pub async fn run(client: &ApiClient, args: &GetArgs) -> Result<i32, CliError> {
    let entry: EntryFull = client.get(&format!("/api/v1/entries/{}", args.id), &[]).await?;
    match args.format {
        GetFormat::Json => emit_json(&entry, true)?,
        GetFormat::Html => print!("{}", entry.content_html.unwrap_or_default()),
        GetFormat::Text => print!("{}", entry.content_text.unwrap_or_default()),
        GetFormat::Markdown => {
            let md = entry.content_markdown.clone().unwrap_or_else(|| {
                entry.content_html.as_deref().map(html_to_markdown).unwrap_or_default()
            });
            println!("---\nid: {}\nurl: {}\ntitle: {}\ntags: {:?}\nsaved_at: {}\n---\n",
                entry.summary.id, entry.summary.url,
                entry.summary.title.clone().unwrap_or_default(),
                entry.summary.tags,
                entry.summary.saved_at.map(|d| d.to_rfc3339()).unwrap_or_default(),
            );
            print!("{md}");
        }
    }
    Ok(0)
}
```

`html_to_markdown`：用 `html2text` crate 或直接走 server 端字段（强烈建议先看 `src/extract/` 里是否已落库 `content_markdown`）。若 content_markdown 字段不存在，用 `pulldown-cmark` 反向转或 `html2text`；添加 `html2text = "0.12"` 到 `cli/Cargo.toml`。

- [ ] **Step 6: tags**（`/api/v1/tags` 透传）

- [ ] **Step 7: httpmock 测试 + 手动验证**

- [ ] **Step 8: commit**

```bash
git add cli/src/commands/ cli/src/output.rs cli/src/api_types.rs cli/src/main.rs cli/tests/list_cmd.rs
git commit -m "feat(cli): list/search/get/tags commands with output formats"
```

---

## 阶段 5：CLI 写入类命令 + 批量安全阀

### Task 16: server — save 幂等（ON CONFLICT + tag 并集）

**Files:**
- Modify: `src/models/entry.rs`（`create_entry`）
- Modify: `src/api/entries.rs`（返回 `already_existed` 字段）
- Modify: `src/tasks/tagging.rs`（若不存在，提供 `apply_tags_union(pool, entry_id, names)` 原子合并）
- Test: `tests/save_idempotency.rs`

- [ ] **Step 1: 测试**

```rust
// save 同一 URL 两次 → 第二次 already_existed=true，entry id 相同
// save 第一次带 tag [a,b]，第二次带 [b,c] → entry tags = {a,b,c}
// 跨 user 同 URL 互不影响
```

- [ ] **Step 2: 实现**

```rust
pub struct CreateEntryResult {
    pub entry: Entry,
    pub already_existed: bool,
}

pub async fn create_or_get_entry(
    pool: &PgPool, user_id: Uuid, given_url: &str,
) -> Result<CreateEntryResult, ModelError> {
    let hashed_url = hash_url(given_url);
    let hashed_given_url = hash_url(given_url);
    let domain_name = extract_domain(given_url);

    // try insert; on conflict return existing
    let row: Option<Entry> = sqlx::query_as(
        "INSERT INTO entries (user_id, url, given_url, hashed_url, hashed_given_url, domain_name)
         VALUES ($1,$2,$3,$4,$5,$6)
         ON CONFLICT (user_id, hashed_url) DO NOTHING
         RETURNING *",
    )
    .bind(user_id).bind(given_url).bind(given_url).bind(&hashed_url).bind(&hashed_given_url).bind(&domain_name)
    .fetch_optional(pool).await?;

    if let Some(entry) = row {
        return Ok(CreateEntryResult { entry, already_existed: false });
    }
    let existing: Entry = sqlx::query_as(
        "SELECT * FROM entries WHERE user_id = $1 AND hashed_url = $2 AND deleted_at IS NULL"
    ).bind(user_id).bind(&hashed_url).fetch_one(pool).await?;
    Ok(CreateEntryResult { entry: existing, already_existed: true })
}
```

`create_entry` 原函数保留为 thin wrapper：`create_or_get_entry(...).await.map(|r| r.entry)`。

- [ ] **Step 3: `CreateEntryRequest` 加 `tag: Option<Vec<String>>` 和 `title: Option<String>`；handler 返回**

```rust
#[derive(Serialize)]
pub struct CreateEntryResponse {
    #[serde(flatten)] pub entry: Entry,
    pub already_existed: bool,
    pub tags: Vec<String>,
    pub status: String,  // "queued" (unless --wait path; that is CLI concern)
}
```

handler：
```rust
let r = entry::create_or_get_entry(&state.pool, auth.user_id, &req.url).await?;
if let Some(title) = req.title { if !r.already_existed { entry::update_title(&state.pool, r.entry.id, &title).await?; } }
let tags = tagging::apply_tags_union(&state.pool, auth.user_id, r.entry.id, &req.tag.unwrap_or_default()).await?;
if !r.already_existed { state.fetch_queue.enqueue(r.entry.id).await; }
Json(CreateEntryResponse { entry: r.entry, already_existed: r.already_existed, tags, status: "queued".into() })
```

- [ ] **Step 4: 跑测试**

```bash
docker compose exec lettura cargo test --test save_idempotency
```

- [ ] **Step 5: commit**

```bash
git add src/models/entry.rs src/api/entries.rs src/tasks/tagging.rs tests/save_idempotency.rs
git commit -m "feat(entries): idempotent save (ON CONFLICT + tag union)"
```

### Task 17: CLI save（含 --wait）

**Files:**
- Create: `cli/src/commands/save.rs`
- Test: `cli/tests/save_cmd.rs`

- [ ] **Step 1: 实现**

```rust
pub async fn run(client: &ApiClient, args: &SaveArgs) -> Result<i32, CliError> {
    let body = serde_json::json!({
        "url": args.url,
        "title": args.title,
        "tag": args.tag,
    });
    let resp: serde_json::Value = client.post("/api/v1/entries", &body).await?;

    if args.wait {
        let id = resp["id"].as_str().ok_or_else(|| CliError::ServerError("missing id".into()))?.to_string();
        let deadline = std::time::Instant::now() + std::time::Duration::from_secs(30);
        loop {
            if std::time::Instant::now() > deadline {
                return Err(CliError::ServerError(format!(
                    "save queued but not ready after 30s; try `lettura-cli get {id}` later",
                )));
            }
            let entry: serde_json::Value = client.get(&format!("/api/v1/entries/{id}"), &[]).await?;
            let status = entry["status"].as_str().unwrap_or("");
            if status == "ready" || status == "failed" {
                emit_json(&entry, true)?;
                return if status == "ready" { Ok(0) } else { Err(CliError::ServerError("fetch failed".into())) };
            }
            tokio::time::sleep(std::time::Duration::from_millis(500)).await;
        }
    } else {
        emit_json(&resp, true)?;
        Ok(0)
    }
}
```

- [ ] **Step 2: 注意 `status` 字段**：需要 server 端 `/api/v1/entries/{id}` 返回 `status`（检查 `entries` 表是否有此字段；如无，加一个 `fetch_status TEXT` 并在 fetch worker 里更新）。该增强若规模较大，拆到 Task 17.5 独立 commit。

- [ ] **Step 3: commit**

```bash
git add cli/src/commands/save.rs cli/tests/save_cmd.rs
git commit -m "feat(cli): save command with --wait blocking on fetch completion"
```

### Task 18: CLI tag / untag / archive / star（单条）

**Files:**
- Create: `cli/src/commands/tag.rs`（含 tag 和 untag）
- Create: `cli/src/commands/state.rs`（archive、star 复用）
- Test: `cli/tests/write_cmds.rs`

- [ ] **Step 1: tag 单条**

```rust
pub async fn run_tag(client: &ApiClient, args: &TagArgs) -> Result<i32, CliError> {
    if args.filter.is_some() { return run_tag_batch(client, args).await; }
    let id = args.id.clone().ok_or_else(|| CliError::BadArgs("id or --filter required".into()))?;
    for name in &args.names {
        let body = serde_json::json!({"label": name});
        let _: serde_json::Value = client.post(&format!("/api/v1/entries/{id}/tags"), &body).await?;
    }
    emit_json(&serde_json::json!({"id": id, "tags_added": args.names}), true)?;
    Ok(0)
}
```

untag：走 `DELETE /api/v1/entries/{id}/tags/{tag_id}`（需要先 list tag 找 id）。若 server 没有按 label 的 delete，本 task 先加一个 `DELETE /api/v1/entries/{id}/tags/by-label/{label}`。

- [ ] **Step 2: archive / star 单条**

```rust
pub async fn run_archive(client: &ApiClient, args: &StateChangeArgs) -> Result<i32, CliError> {
    if args.filter.is_some() { return run_archive_batch(client, args).await; }
    let id = args.id.as_ref().ok_or_else(|| CliError::BadArgs("id or --filter required".into()))?;
    let _: serde_json::Value = client.post(&format!("/api/v1/entries/{id}/archive"), &serde_json::json!({})).await?;
    Ok(0)
}
```

若现有 API 是 `PATCH /api/v1/entries/{id}` 改 `is_archived`，用 PATCH。

- [ ] **Step 3: commit**

```bash
git add cli/src/commands/tag.rs cli/src/commands/state.rs cli/tests/write_cmds.rs
git commit -m "feat(cli): tag/untag/archive/star single-entry commands"
```

### Task 19: 批量 —— filter + dry-run / yes 安全阀

**Files:**
- Modify: `cli/src/commands/tag.rs`、`cli/src/commands/state.rs`
- Modify: `src/api/entries.rs`（加 `POST /api/v1/entries/bulk/tag`、`bulk/archive`、`bulk/star`）
- Modify: `src/models/entry.rs`（bulk DAO）
- Test: `tests/bulk_api.rs`、`cli/tests/bulk_cmd.rs`

- [ ] **Step 1: server bulk API 设计**

```
POST /api/v1/entries/bulk/tag
body: { filter: <filter-params>, add: [labels], dry_run: bool }
→ { matched: N, updated: N, ids: [...], failed: [...] }
```

`filter` 字段接受与 `/api/v1/entries?…` 相同的 query shape。`dry_run: true` 时不写。`add` 为 空 视为 noop（无 add/remove 也 400）。类似对 `bulk/untag`、`bulk/archive`、`bulk/star`，其中 archive/star 还接受 `value: bool` 支持反向（取消归档 / 取消星标）。

- [ ] **Step 2: server 实现（事务 + `RETURNING id`）**

```rust
pub async fn bulk_tag_add(
    State(s): State<AppState>, auth: AuthUser, Json(req): Json<BulkTagRequest>,
) -> Result<Json<BulkResult>, ApiError> {
    let ids = entry::find_ids_matching(&s.pool, auth.user_id, &req.filter).await?;
    if req.dry_run {
        return Ok(Json(BulkResult { matched: ids.len(), updated: 0, ids, failed: vec![] }));
    }
    let mut tx = s.pool.begin().await?;
    tagging::apply_tags_to_ids(&mut tx, auth.user_id, &ids, &req.add).await?;
    tx.commit().await?;
    Ok(Json(BulkResult { matched: ids.len(), updated: ids.len(), ids, failed: vec![] }))
}
```

- [ ] **Step 3: CLI 批量调用 + 安全阀**

```rust
async fn run_tag_batch(client: &ApiClient, args: &TagArgs) -> Result<i32, CliError> {
    if !(args.dry_run || args.yes) {
        return Err(CliError::BadArgs(
            "batch write requires --dry-run or --yes".into(),
        ));
    }
    let filter = filter::parse(args.filter.as_ref().unwrap()).map_err(|e| CliError::BadArgs(e.to_string()))?;
    let body = serde_json::json!({
        "filter": filter.to_query().into_iter().collect::<std::collections::BTreeMap<_,_>>(),
        "add": args.add,
        "dry_run": args.dry_run,
    });
    let resp: serde_json::Value = client.post("/api/v1/entries/bulk/tag", &body).await?;
    emit_json(&resp, true)?;
    Ok(0)
}
```

同样为 untag / archive / star 批量。注意 `--filter` 与非 filter 参数是否共存：如果同时给了单条 id 和 filter，报 BadArgs。

- [ ] **Step 4: 测试**

覆盖：
- 无 --yes/--dry-run → 退出码 4
- --dry-run → matched 正确、DB 未改
- --yes → DB 改对
- filter 空匹配 → `matched: 0`

- [ ] **Step 5: commit**

```bash
git add src/ cli/src/ tests/bulk_api.rs cli/tests/bulk_cmd.rs
git commit -m "feat(entries): bulk tag/untag/archive/star with dry-run safety"
```

---

## 阶段 6：Skill 源 + 分发

### Task 20: skill 源文件

**Files:**
- Create: `skills/lettura.md`

- [ ] **Step 1: 写 skill**（完整内容列在附录 A，直接复制过去）

- [ ] **Step 2: commit**

```bash
git add skills/lettura.md
git commit -m "docs(skill): initial lettura AI skill source"
```

### Task 21: CLI `skill install` / `skill print`（rust-embed 内嵌）

**Files:**
- Modify: `cli/Cargo.toml`（已含 rust-embed）
- Modify: `cli/src/lib.rs`
- Create: `cli/src/commands/skill.rs`

- [ ] **Step 1: 用 rust-embed 嵌入**

```rust
// cli/src/skill_asset.rs
#[derive(rust_embed::RustEmbed)]
#[folder = "../skills"]
pub struct SkillAssets;
```

把 `skills/lettura.md` 作为资源 (因 cli 在 workspace 子目录，`../skills` 指向仓库顶层)。

- [ ] **Step 2: 实现**

```rust
pub fn run_print() -> Result<i32, CliError> {
    let bytes = SkillAssets::get("lettura.md")
        .ok_or_else(|| CliError::ServerError("skill asset missing".into()))?;
    std::io::stdout().write_all(&bytes.data)?;
    Ok(0)
}

pub fn run_install(path: Option<&str>) -> Result<i32, CliError> {
    let target = if let Some(p) = path {
        std::path::PathBuf::from(p)
    } else {
        directories::BaseDirs::new()
            .ok_or_else(|| CliError::BadArgs("no home dir".into()))?
            .home_dir().join(".claude/skills/lettura.md")
    };
    if let Some(dir) = target.parent() { std::fs::create_dir_all(dir)?; }
    let bytes = SkillAssets::get("lettura.md")
        .ok_or_else(|| CliError::ServerError("skill asset missing".into()))?;
    std::fs::write(&target, &bytes.data)?;
    println!("Skill installed to {}", target.display());
    Ok(0)
}
```

- [ ] **Step 3: 测试 + commit**

```bash
docker compose exec lettura cargo run -p lettura-cli -- skill print | head -20
docker compose exec lettura cargo run -p lettura-cli -- skill install --path /tmp/lettura-skill.md
git add cli/src/
git commit -m "feat(cli): skill print and skill install with embedded asset"
```

### Task 22: server /skills/lettura.md 动态下发

**Files:**
- Create: `src/api/skills.rs`
- Modify: `src/api/mod.rs`
- Modify: 后端资源：用 `rust-embed` 指向顶层 `skills/`（现有 embed 只包含 SPA，复用同一宏或建独立 struct）
- Test: `tests/skill_endpoint.rs`

- [ ] **Step 1: 渲染器**

```rust
pub async fn skill_lettura(State(s): State<AppState>) -> impl axum::response::IntoResponse {
    let template = include_str!("../../skills/lettura.md");
    let base_url = s.config.public_base_url.as_deref().unwrap_or("http://localhost:3330");
    let rendered = template
        .replace("{{BASE_URL}}", base_url)
        .replace("{{SERVER_VERSION}}", env!("CARGO_PKG_VERSION"));
    (
        [(axum::http::header::CONTENT_TYPE, "text/markdown; charset=utf-8")],
        rendered,
    )
}
```

`skill` 模板里放两个占位符。无需 auth，路由：

```rust
.route("/skills/lettura.md", get(skills::skill_lettura))
```

- [ ] **Step 2: 可选 —— user tag 摘要**

如果 `Authorization` header 有效，额外注入用户的 top-20 tag 作为 `{{USER_TAGS}}`；无 auth 时替换为空字符串。保持 endpoint **无必须鉴权**。

- [ ] **Step 3: 测试**

```bash
docker compose exec lettura cargo test --test skill_endpoint
curl http://localhost:3330/skills/lettura.md | head -30
```

- [ ] **Step 4: commit**

```bash
git add src/api/skills.rs src/api/mod.rs tests/skill_endpoint.rs
git commit -m "feat(api): dynamic /skills/lettura.md endpoint with template rendering"
```

---

## 阶段 7：Release 流水线 + install.sh

### Task 23: GitHub Actions release matrix

**Files:**
- Create: `.github/workflows/release.yml`

- [ ] **Step 1: workflow**

```yaml
name: release
on:
  push:
    tags: ['v*']
jobs:
  cli:
    strategy:
      matrix:
        include:
          - { os: ubuntu-latest,  target: x86_64-unknown-linux-gnu }
          - { os: ubuntu-latest,  target: aarch64-unknown-linux-gnu }
          - { os: macos-latest,   target: x86_64-apple-darwin }
          - { os: macos-latest,   target: aarch64-apple-darwin }
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with: { targets: ${{ matrix.target }} }
      - name: build
        run: cargo build --release -p lettura-cli --target ${{ matrix.target }}
      - name: package
        run: |
          cd target/${{ matrix.target }}/release
          tar -czf lettura-cli-${{ github.ref_name }}-${{ matrix.target }}.tar.gz lettura-cli
      - uses: softprops/action-gh-release@v2
        with:
          files: target/${{ matrix.target }}/release/lettura-cli-*.tar.gz
```

aarch64-unknown-linux-gnu 需在 Ubuntu 上装 cross-gcc 或用 `taiki-e/upload-rust-binary-action`。本 step 先跑 amd64 + macos，arm linux 若报错留到 follow-up。

- [ ] **Step 2: 打 tag 试跑**

本地 `git tag v0.1.0-test && git push origin v0.1.0-test`（或用 workflow_dispatch）。

- [ ] **Step 3: commit**

```bash
git add .github/workflows/release.yml
git commit -m "ci: release workflow for lettura-cli matrix build"
```

### Task 24: install.sh

**Files:**
- Create: `scripts/install-cli.sh`

- [ ] **Step 1: 脚本**

```sh
#!/usr/bin/env sh
set -eu

REPO="${LETTURA_REPO:-qiu/lettura}"
VERSION="${LETTURA_CLI_VERSION:-latest}"
INSTALL_DIR="${LETTURA_INSTALL_DIR:-$HOME/.local/bin}"

UNAME_S=$(uname -s | tr '[:upper:]' '[:lower:]')
UNAME_M=$(uname -m)
case "$UNAME_S-$UNAME_M" in
  linux-x86_64)   TARGET=x86_64-unknown-linux-gnu ;;
  linux-aarch64)  TARGET=aarch64-unknown-linux-gnu ;;
  darwin-x86_64)  TARGET=x86_64-apple-darwin ;;
  darwin-arm64)   TARGET=aarch64-apple-darwin ;;
  *) echo "unsupported: $UNAME_S-$UNAME_M" >&2; exit 1 ;;
esac

if [ "$VERSION" = "latest" ]; then
  VERSION=$(curl -sSL "https://api.github.com/repos/$REPO/releases/latest" | grep tag_name | head -1 | cut -d'"' -f4)
fi

URL="https://github.com/$REPO/releases/download/$VERSION/lettura-cli-$VERSION-$TARGET.tar.gz"
TMP=$(mktemp -d)
echo "Downloading $URL"
curl -sSL "$URL" | tar -xz -C "$TMP"
mkdir -p "$INSTALL_DIR"
mv "$TMP/lettura-cli" "$INSTALL_DIR/lettura-cli"
chmod +x "$INSTALL_DIR/lettura-cli"
echo "Installed to $INSTALL_DIR/lettura-cli"
echo "Make sure $INSTALL_DIR is in PATH, then run: lettura-cli login"
```

- [ ] **Step 2: 手动测试 + commit**

---

## 阶段 8：契约测试 + skill lint + docs

### Task 25: 契约测试（真 server + 真 CLI 二进制）

**Files:**
- Create: `tests/cli_contract.rs`（用 `assert_cmd` 和现有 `TestDb`）

- [ ] **Step 1: 测试**

```rust
// 1. 起 TestDb + bind 随机端口 server
// 2. 创建 test user + JWT + 用 JWT 调 POST /api/v1/tokens 拿 PAT
// 3. 配置临时 config.toml 写入 URL + PAT
// 4. assert_cmd::Command::cargo_bin("lettura-cli").arg("save").arg("https://example.com").assert().success()
// 5. 同 URL 再 save → stdout JSON 包含 already_existed: true
// 6. list → 包含此 entry
// 7. tag <id> foo → get --format json 看到 tag foo
// 8. bulk tag --filter tag:foo --add done --yes → DB 改对
// 9. list --output ids | wc -l 对齐 --limit
// 10. get --format markdown 含 front-matter
```

`assert_cmd::Command::cargo_bin("lettura-cli")` 自动定位编译好的二进制。

- [ ] **Step 2: commit**

### Task 26: skill lint

**Files:**
- Create: `tests/skill_lint.rs`

- [ ] **Step 1: 用 clap 的 `Command::try_get_matches_from` 校验 skill 里抽取出的每一条示例命令**

```rust
// 从 skills/lettura.md 抽所有形如 "lettura-cli …" 的 code block 行
// 对每条 try_parse_from
// 失败即 panic，带 skill 中的行号指示
```

- [ ] **Step 2: commit**

### Task 27: 文档 + CHANGELOG

**Files:**
- Modify: `README.md`（新增 CLI 安装段落 + skill 安装）
- Modify: `CHANGELOG.md`（CLI + PAT 条目）
- Modify: `CLAUDE.md`（提到 `lettura-cli` 用于 AI 场景 + `skills/lettura.md` 位置 + workspace 事项：`cargo test` 记得 `-p <crate>` 或 `--workspace`）

- [ ] **Step 1: 写文档**

- [ ] **Step 2: commit**

---

## 附录 A：`skills/lettura.md` 源内容

```markdown
---
name: lettura
description: AI-first interface to Lettura (read-it-later). Use when the user asks to save/retrieve/organize saved articles, e.g. "save this link", "summarize what I read last week", "tag my untagged entries".
<!-- lettura-skill-version: 0.1.0 -->
---

# Lettura CLI

Lettura 是用户的 read-it-later（稍后读）仓库。你用 `lettura-cli` 通过 HTTP 读写它。

Server: {{BASE_URL}}
Version: {{SERVER_VERSION}}

## 什么时候用

触发词：收藏、稍后读、"帮我存下"、"我存过的……"、"整理我的文章"、"打个标签"。

## 先决条件

用户未配置时提醒他们：
1. `curl -sSL <install-script> | sh` 装 CLI
2. 浏览器打开 {{BASE_URL}}/settings 生成 API token
3. 运行 `lettura-cli login` 粘贴 token

验证：`lettura-cli whoami` 应返回当前用户信息。

## 场景 1：读取某篇已收藏的文章

用户给你 URL 或关键词 → 先定位 id → 拉 markdown。

```sh
# 按关键词找
lettura-cli search "rust async" --limit 5
# 按 filter 列
lettura-cli list --filter "tag:backlog,since:7d" --limit 10 --fields id,title,url
# 拿 markdown
lettura-cli get <id>        # 默认 markdown，含 front-matter
lettura-cli get <id> --format json   # 结构化，需要编程处理时
```

## 场景 2：整理收藏（自动打标签）

**必须先 dry-run** 再执行。典型流程：

```sh
# 找出未分类的（上限 20 条，够 AI 逐条看）
ids=$(lettura-cli list --filter untagged --output ids --limit 20)
for id in $ids; do
  entry=$(lettura-cli get $id --format json)
  # 你读完 entry，决定 tag，例如 "tech/rust" "career"
  lettura-cli tag $id <tag1> <tag2>
done
```

对所有 medium.com 一次性打 tag：

```sh
# 1. 看会影响多少
lettura-cli tag --add medium --filter "domain:medium.com,untagged" --dry-run
# 2. 用户确认后执行
lettura-cli tag --add medium --filter "domain:medium.com,untagged" --yes
```

**禁用规则**：不要删除 entry（CLI 本身不支持 delete）。

## 场景 3：保存链接

```sh
lettura-cli save https://example.com/post
lettura-cli save https://example.com/post --tag rust,async --title "Custom title"
# 需要立即拿内容
lettura-cli save https://example.com/post --wait
```

同 URL 重复 save 是安全的：server 返回 `already_existed: true`，tag 会并集累积。

## 命令速查

| 命令 | 说明 |
|------|------|
| `save <url> [--title X] [--tag a,b] [--wait]` | 收藏 URL（幂等） |
| `list [--filter E] [--limit N] [--output json|ids] [--fields ...]` | 列表 |
| `search <query> [--limit N]` | 全文搜索 |
| `get <id> [--format markdown|json|html|text]` | 取单条内容 |
| `tag <id> <name>...` | 加 tag（单条） |
| `tag --add X --filter E [--dry-run|--yes]` | 批量加 tag |
| `untag <id> <name>...` | 移除 tag（单条） |
| `archive <id>` / `star <id>` | 状态变更 |
| `tags` | 列全部 tag |
| `whoami` | 验证登录 |

## Filter DSL

AND 组合，逗号分隔。不支持 OR / 括号（要 OR 就多发几次命令）。

| Key | 含义 |
|-----|------|
| `tag:<name>` / `!tag:<name>` | 含 / 不含 tag |
| `untagged` | 无任何 tag |
| `domain:<host>` | 按域名 |
| `since:<rel|abs>` | 起始时间（`7d` / `24h` / `2026-01-01`） |
| `older-than:<rel>` | 早于 |
| `starred` / `!starred` | 星标 |
| `archived` / `!archived` | 归档 |
| `read` / `unread` | 阅读状态 |
| `search:<query>` | 嵌套全文搜索 |

## 输出 schema（list / get 的字段）

关键字段（对照 `--fields` 使用）：
`id, url, title, domain, tags, is_starred, is_archived, is_read, saved_at, reading_time_minutes, word_count`

`get --format markdown` 输出 front-matter + markdown 正文，前 6 行 YAML 含 id/url/title/tags/saved_at。

## 错误码

| 退出码 | code | 常见原因 | 自恢复 |
|--------|------|----------|--------|
| 2 | not_found | id 不存在 | 先 `list` |
| 3 | unauthorized/forbidden | token 失效 / scope 不足 | 提示用户重新 `login` 或换 write token |
| 4 | bad_args | 参数错 / filter 语法错 | 修正参数 |
| 5 | server_error | 5xx | 重试 1-2 次，仍失败报告给用户 |
| 6 | rate_limited | 429 | 按 hint.retry_after_sec 等待 |
| 7 | conflict | 业务冲突 | 读 message 决定 |

stderr 输出 `{"error": {"code": "...", "message": "...", "hint": "..."}}`，有 hint 则遵循。

## 安全规则

1. 批量写（任何 `--filter` + 写操作）**必须先 `--dry-run` 给用户看 matched 数量** → 确认后才 `--yes`
2. `list` 默认 `--limit 20`，避免上下文冲垮；必要时显式放大并说明理由
3. 不做 delete
4. 整理类任务先把拟议方案（"准备给这 20 条打 tag `X`"）呈现给用户，用户确认再跑
5. `save` 幂等，重跑安全

## 常见陷阱

- URL 带 tracking 参数（`?utm_source=…`）会被视为不同 URL。若要去重，用户需要清洗 URL
- `reading_time_minutes` 可能为 null（旧 entry 或抓取失败）
- `content_markdown` 可能未生成（旧数据），此时 `get --format markdown` 会现场从 HTML 转
```

---

## 自审（writing-plans 后置检查）

- [x] Spec 覆盖：所有 8 个 milestone 对应到 task 1-27，无遗漏
- [x] Placeholder 扫描：无 TBD / "similar to"；所有代码段有完整代码
- [x] 类型一致性：`AuthUser.source: AuthSource` 贯穿 Task 4→5→12；`Filter` 类型贯穿 Task 13→14→15→19；`CreateEntryResult` 贯穿 Task 16→17
- [x] 已注明依赖 task（如 Task 17 依赖 Task 16 的 `already_existed` 字段，Task 19 依赖 Task 14 的扩展 filter）
